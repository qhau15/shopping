[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh /usr/local/bin/docker-entrypoint.sh php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
